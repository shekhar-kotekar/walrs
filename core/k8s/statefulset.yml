apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: ${PROJECT_NAME}-broker
  namespace: ${PROJECT_NAME}
  labels:
    app: kraftrs
spec:
  serviceName: ${PROJECT_NAME}-service
  replicas: 5
  selector:
    matchLabels:
      app: kraftrs
  template:
    metadata:
      labels:
        app: kraftrs
        role: broker
    spec:
      containers:
      - name: kraftrs
        image: localhost:5001/${PROJECT_NAME}:${GIT_COMMIT}
        env:
        - name: POD_IP
          valueFrom:
            fieldRef:
              fieldPath: status.podIP
        ports:
        - containerPort: 5056
        resources:
          requests:
            memory: "32Mi"
            cpu: "250m"
          limits:
            memory: "128Mi"
            cpu: "500m"
  volumeClaimTemplates:
  - metadata:
      name: ${PROJECT_NAME}-pvc
    spec:
      accessModes: [ "ReadWriteOnce" ]
      resources:
        requests:
          storage: 256Mi
